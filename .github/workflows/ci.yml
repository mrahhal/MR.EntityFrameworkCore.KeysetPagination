name: CI

on:
  workflow_dispatch:
  push:
    branches:
    - main
    tags:
    - v*
  pull_request:
    branches:
    - main

jobs:
  main:
    uses: mrahhal/.github/workflows/ci-dotnet-package.yml@main
    with:
      dotnet-version: 6.x.x
    secrets:
      NUGET_ORG_API_KEY: ${{ secrets.NUGET_ORG_API_KEY }}
